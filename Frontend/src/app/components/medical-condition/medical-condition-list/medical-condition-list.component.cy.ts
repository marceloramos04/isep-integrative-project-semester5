import { MedicalConditionListComponent } from './medical-condition-list.component'

describe('MedicalConditionListComponent', () => {
  it('should mount', () => {
    cy.mount(MedicalConditionListComponent)
  })
})